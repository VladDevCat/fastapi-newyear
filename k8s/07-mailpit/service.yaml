apiVersion: v1
kind: Service
metadata:
  name: mailpit
  namespace: wp-labs
  labels:
    app: mailpit
spec:
  type: ClusterIP
  selector:
    app: mailpit
  ports:
    - port: 1025
      targetPort: smtp
      name: smtp
    - port: 8025
      targetPort: http
      name: http
